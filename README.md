# CodeAlpha_ProjectName
A simple rule-based chatbot built in Python that interacts with users through predefined responses. The chatbot accepts user input, matches it against a dictionary of known commands, and returns appropriate replies. It runs continuously until the user enters "bye" to exit. 
